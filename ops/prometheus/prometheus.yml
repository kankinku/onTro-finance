global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: ontro-finance
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000
